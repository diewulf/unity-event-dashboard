// ARCHIVO GENERADO AUTOMÁTICAMENTE - NO EDITAR MANUALMENTE
// Fuente: GameState.enum.cs

export enum GameState {
    GAMEPLAY = "GAMEPLAY",
    eventos ejecutándose
    PAUSED = "eventos ejecutándose
    PAUSED"
}

export const GAMESTATE_TYPES = Object.values(GameState) as GameState[];
