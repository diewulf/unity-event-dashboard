// ARCHIVO GENERADO AUTOMÁTICAMENTE - NO EDITAR MANUALMENTE
// Fuente: GameState.enum.cs

export enum GameState {
    GAMEPLAY = "GAMEPLAY",
    CUTSCENE = "CUTSCENE",
    PAUSED = "PAUSED",
    DIALOGUE = "DIALOGUE"
}

export const GAME_STATE = Object.values(GameState) as GameState[];
